#!/bin/sh

# Check for code style violations with ktlint
./gradlew ktlintFormat

# If there are any violations, exit with a non-zero status code
# to prevent the commit from being made
if [ $? -ne 0 ]; then
  echo "Code style violations found. Please fix them and try again."
  exit 1
fi